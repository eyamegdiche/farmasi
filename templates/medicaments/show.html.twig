{% extends 'template.html.twig' %}

{% block title %}Medicaments{% endblock %}

{% block body %}
    <h1>Medicaments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medicament.id }}</td>
            </tr>
            <tr>
                <th>Pa</th>
                <td>{{ medicament.pa }}</td>
            </tr>
            <tr>
                <th>Pv</th>
                <td>{{ medicament.pv }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ medicament.qte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('Medicaments_index') }}">back to list</a>

    <a href="{{ path('Medicaments_edit', {'id': medicament.id}) }}">edit</a>

    {{ include('Medicaments/_delete_form.html.twig') }}
{% endblock %}
