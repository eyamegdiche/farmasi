{% extends 'template.html.twig' %}

{% block title %}Medicaments index{% endblock %}

{% block body %}
    <h1>Medicaments index</h1>

    <table class="table table-condensed">
        <thead>
            <tr>
                <th>id de medicament</th>
                <th>Prix d'achat</th>
                <th>Prix de vente</th>
                <th>Quantité</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for medicament in Medicaments %}
            <tr>
                
                <td>{{ medicament.pa }}</td>
                <td>{{ medicament.pv }}</td>
                <td>{{ medicament.qte }}</td>
                 <td><img src ="{{ asset('uploads/images/' ~ produit.image) }}"
   alt =""  width="100" height="100"></td>
                <td>
                    <a href="{{ path('Medicaments_show', {'id': medicament.id}) }} "class = "btn btn-info">afficher</a>
                    <a href="{{ path('Medicaments_edit', {'id': medicament.id}) }}"class = "btn btn-warning">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">il n'ya pas de medicaments</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('Medicaments_new') }}" class = "btn btn-success">Ajouter</a>
{% endblock %}
